#include <stdio.h>

int main() {

   const int MAX = 80;  
   char str[MAX];  

   printf("\nEnter a string: ");
   gets(str);  // get string input 

   printf("The string is:\n");
   puts(str);  // display the string

   return 0;

}
