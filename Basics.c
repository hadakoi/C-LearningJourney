#include <stdio.h> // This is the file header and should be included on all c files.

// This is a comment using the representation of "//" and we can also use 

/*
To Create Multiple Line Comments
As can be Seen
*/

int main(){ //this represents the main body of code to be executed

  int x; // basic declaration of making the x variable an integer

  x = 5; // Adds a value to x

  printf("%d", x); // Prints the variable x

  return 0; // shows its the end of the code
}

//Important note to add ; after every Line of Code
